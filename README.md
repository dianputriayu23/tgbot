# PKEU College Schedule Telegram Bot

Телеграм-бот для просмотра расписания занятий колледжа PKEU.

## Возможности

- 📅 Просмотр расписания на сегодня, завтра или конкретный день недели
- 🔔 Настройка уведомлений о парах и изменениях в расписании
- 🔄 Автоматическое обновление расписания с официального сайта
- 📊 Поддержка разных форм обучения (на базе 9 и 11 классов)
- ⚙️ Персональные настройки для каждого пользователя

## Требования

- Python 3.8 или выше
- Токен Telegram бота (получить у [@BotFather](https://t.me/BotFather))

## Установка

1. Клонируйте репозиторий:
```bash
git clone https://github.com/dianputriayu23/tgbot.git
cd tgbot
```

2. Создайте виртуальное окружение и активируйте его:
```bash
python -m venv venv

# На Windows:
venv\Scripts\activate

# На Linux/Mac:
source venv/bin/activate
```

3. Установите зависимости:
```bash
pip install -r requirements.txt
```

4. Создайте файл `.env` на основе `.env.example`:
```bash
cp .env.example .env
```

5. Отредактируйте `.env` и добавьте ваш токен бота:
```
API_TOKEN=ваш_токен_от_BotFather
ADMIN_ID=ваш_telegram_id
```

## Запуск

```bash
python main.py
```

## Структура проекта

```
tgbot/
├── database/          # Работа с базой данных SQLite
│   ├── __init__.py
│   └── db.py         # Модель данных и CRUD операции
├── handlers/          # Обработчики команд и сообщений
│   ├── __init__.py
│   ├── common.py     # Основные команды (/start, /help, настройки)
│   └── schedule_viewer.py  # Просмотр расписания
├── keyboards/         # Клавиатуры для бота
│   ├── __init__.py
│   ├── inline.py     # Inline-клавиатуры
│   └── reply.py      # Reply-клавиатуры
├── parser/            # Парсинг XLSX файлов с расписанием
│   ├── __init__.py
│   └── parser.py     # Загрузка и парсинг расписания
├── scheduler/         # Планировщик задач
│   ├── __init__.py
│   └── jobs.py       # Периодические проверки расписания
├── utils/             # Вспомогательные модули
│   ├── __init__.py
│   └── states.py     # FSM состояния для регистрации
├── main.py           # Точка входа в приложение
├── requirements.txt  # Зависимости Python
├── .env.example      # Пример файла конфигурации
└── README.md         # Этот файл
```

## Команды бота

- `/start` - Начало работы и регистрация
- `Сегодня` - Расписание на сегодня
- `Завтра` - Расписание на завтра
- `Понедельник...Суббота` - Расписание на конкретный день
- `⚙️ Настройки` - Изменение группы и уведомлений
- `❓ Помощь` - Список команд и информация

## Технологии

- **aiogram 3.x** - Асинхронный фреймворк для Telegram Bot API
- **aiosqlite** - Асинхронная работа с SQLite
- **APScheduler** - Планировщик задач для автоматического обновления
- **python-calamine** - Быстрый парсинг XLSX файлов
- **BeautifulSoup4** - Парсинг HTML для загрузки файлов с сайта
- **python-dotenv** - Управление переменными окружения

## Автоматическое обновление расписания

Бот автоматически проверяет наличие нового расписания на сайте PKEU каждые 30 минут. При обнаружении нового файла, расписание автоматически обновляется в базе данных.

## Разработка

Для разработки рекомендуется использовать следующие инструменты:
- Линтер: `flake8` или `pylint`
- Форматирование кода: `black`
- Тестирование: `pytest`

## Лицензия

MIT License

## Автор

dianputriayu23

## Поддержка

Если у вас возникли вопросы или проблемы, создайте issue в репозитории.