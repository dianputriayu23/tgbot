# Telegram бот для расписания колледжа

Полнофункциональный Telegram бот для просмотра расписания занятий студентами Пермского колледжа экономики и управления.

## Возможности

✅ **Просмотр расписания**
- Расписание на сегодня, завтра
- Расписание на неделю
- Расписание на конкретную дату

✅ **Уведомления**
- Автоматическая отправка расписания в выбранное время
- Настраиваемое время уведомлений
- Включение/выключение уведомлений

✅ **Управление профилем**
- Выбор курса и группы
- Просмотр информации о профиле
- Изменение настроек

✅ **Парсинг Excel**
- Поддержка различных форматов XLSX
- Обработка плохо отформатированных файлов
- Автоматическое обновление расписания

## Установка

### Требования

- Python 3.10 или выше
- pip (менеджер пакетов Python)

### Шаги установки

1. **Клонируйте репозиторий**
```bash
git clone <repository-url>
cd tgbot
```

2. **Установите зависимости**
```bash
pip install -r requirements.txt
```

3. **Создайте файл конфигурации**
```bash
cp .env.example .env
```

4. **Настройте .env файл**

Откройте `.env` и укажите ваш токен бота:
```env
BOT_TOKEN=your_bot_token_here
```

Для получения токена:
- Напишите [@BotFather](https://t.me/BotFather) в Telegram
- Создайте нового бота командой `/newbot`
- Скопируйте полученный токен

5. **Добавьте файлы расписания**

Поместите файлы `.xlsx` с расписанием в корневую папку проекта.

## Запуск

### Обычный запуск
```bash
python main.py
```

### Запуск в фоновом режиме (Linux)
```bash
nohup python main.py &
```

### Запуск с использованием systemd (Linux)

Создайте файл `/etc/systemd/system/tgbot.service`:
```ini
[Unit]
Description=Telegram Schedule Bot
After=network.target

[Service]
Type=simple
User=youruser
WorkingDirectory=/path/to/tgbot
ExecStart=/usr/bin/python3 /path/to/tgbot/main.py
Restart=always

[Install]
WantedBy=multi-user.target
```

Запустите:
```bash
sudo systemctl enable tgbot
sudo systemctl start tgbot
sudo systemctl status tgbot
```

## Структура проекта

```
tgbot/
├── main.py                 # Точка входа приложения
├── requirements.txt        # Зависимости Python
├── .env.example           # Пример файла конфигурации
├── .gitignore             # Игнорируемые файлы
├── database/
│   ├── __init__.py
│   └── db.py              # SQLite база данных
├── parser/
│   ├── __init__.py
│   └── parser.py          # Парсер Excel файлов
├── keyboards/
│   ├── __init__.py
│   ├── main.py            # Главная клавиатура
│   └── selection.py       # Клавиатуры выбора
├── handlers/
│   ├── __init__.py
│   ├── start.py           # Обработчик /start
│   ├── schedule.py        # Вывод расписания
│   ├── settings.py        # Настройки
│   ├── profile.py         # Профиль
│   └── help.py            # Помощь
└── scheduler/
    ├── __init__.py
    └── jobs.py            # Уведомления и обновления
```

## Использование

### Для пользователей

1. **Начало работы**: Отправьте `/start` боту
2. **Выбор группы**: Выберите курс и группу
3. **Просмотр расписания**: Используйте кнопки меню
4. **Настройка уведомлений**: Перейдите в настройки

### Команды бота

- `/start` - Начать работу с ботом
- `/help` - Показать справку
- `/changegroup` - Изменить группу
- `/notifications` - Настроить уведомления
- `/about` - О боте

### Главное меню

- **📅 Моё расписание** - Просмотр расписания на выбранный период
- **📆 Расписание на дату** - Быстрый просмотр на конкретную дату
- **👤 Профиль** - Информация о профиле
- **⚙️ Настройки** - Изменение настроек
- **❓ Помощь** - Справка по использованию

## Конфигурация

### Переменные окружения (.env)

```env
# Токен бота Telegram
BOT_TOKEN=your_bot_token_here

# Путь к базе данных SQLite
DATABASE_PATH=schedule.db

# Время уведомлений по умолчанию (24-часовой формат)
NOTIFICATION_TIME=08:00

# Папка с файлами расписания
SCHEDULE_FOLDER=.

# Шаблон файлов расписания
SCHEDULE_FILE_PATTERN=*.xlsx

# Уровень логирования (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Файл логов
LOG_FILE=bot.log

# ID администраторов (через запятую)
ADMIN_IDS=123456789,987654321
```

## Формат файлов расписания

Бот поддерживает Excel файлы (.xlsx) со следующей структурой:

- Несколько листов (по курсам)
- Заголовки с названиями групп
- Столбцы: предмет, аудитория (для каждой группы)
- Строки с днями недели, парами, предметами и преподавателями

Пример структуры:
```
| День       | Пара              | БУ1-24         | ауд. | Ф1-24  | ауд. |
|------------|-------------------|----------------|------|--------|------|
| понедельник| III пара 12:30-13:50 | Математика  | 301  | Физика | 402  |
|            |                   | Иванов И.И.    |      | Петров |      |
```

## Обновление расписания

Бот автоматически проверяет новые файлы расписания каждый час.

Для ручного обновления можно добавить команду администратора (опционально).

## Логирование

Логи сохраняются в:
- Консоль (stdout)
- Файл `logs/bot.log`

Уровни логирования:
- `INFO` - стандартная информация
- `WARNING` - предупреждения
- `ERROR` - ошибки

## Устранение неполадок

### Бот не запускается
- Проверьте, что установлены все зависимости: `pip install -r requirements.txt`
- Убедитесь, что в `.env` указан правильный токен
- Проверьте логи в `logs/bot.log`

### Расписание не отображается
- Убедитесь, что файлы `.xlsx` находятся в папке, указанной в `SCHEDULE_FOLDER`
- Проверьте формат файлов Excel
- Посмотрите логи на наличие ошибок парсинга

### Уведомления не приходят
- Проверьте, что уведомления включены в настройках
- Убедитесь, что указано правильное время
- Проверьте, что scheduler запущен (в логах должно быть "Scheduler started")

## Разработка

### Добавление новых обработчиков

1. Создайте файл в `handlers/`
2. Импортируйте в `handlers/__init__.py`
3. Зарегистрируйте в `main.py`

### Добавление новых команд

```python
from telegram.ext import CommandHandler

async def my_command(update: Update, context: ContextTypes.DEFAULT_TYPE):
    await update.message.reply_text("Ответ")

application.add_handler(CommandHandler('mycommand', my_command))
```

### Тестирование

Рекомендуется создать отдельного тестового бота для разработки.

## Безопасность

⚠️ **Важно:**
- Никогда не коммитьте `.env` файл в git
- Храните токен бота в безопасности
- Регулярно обновляйте зависимости

## Лицензия

Этот проект создан для использования в образовательных целях.

## Автор

Разработано для студентов Пермского колледжа экономики и управления.

## Поддержка

При возникновении проблем или вопросов:
1. Проверьте раздел "Устранение неполадок"
2. Изучите логи в `logs/bot.log`
3. Создайте issue в репозитории

---

**Версия:** 1.0.0  
**Дата:** 2025  
**Python:** 3.10+
