# Руководство по запуску Telegram бота

## Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка конфигурации

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Откройте `.env` и замените `your_bot_token_here` на токен вашего бота:

```env
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
DATABASE_PATH=bot_database.db
SCHEDULE_DIR=.
NOTIFICATION_TIME=18:00
```

### 3. Получение токена бота

1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Введите имя для вашего бота (например: "College Schedule Bot")
4. Введите username для бота (должен заканчиваться на 'bot', например: "college_schedule_bot")
5. BotFather пришлет вам токен вида `123456789:ABCdefGHIjklMNOpqrsTUVwxyz`
6. Скопируйте этот токен в файл `.env`

### 4. Запуск бота

```bash
python main.py
```

Вы должны увидеть:

```
INFO - Starting Telegram Bot for College Schedule
INFO - Database: bot_database.db
INFO - Schedule directory: .
INFO - Database initialized
INFO - Setting up handlers
INFO - Handlers configured
INFO - Scheduler started
INFO - Bot started. Press Ctrl+C to stop.
```

### 5. Добавление расписания

Поместите файлы XLSX с расписанием в директорию бота (или укажите другую директорию в `SCHEDULE_DIR`).

Бот автоматически:
- Загрузит расписание при запуске
- Будет обновлять расписание каждый день в 03:00

## Структура XLSX файлов

Бот поддерживает файлы со следующей структурой:

### Листы
- Должны называться "1 курс", "2 курс", "3 курс"
- База 9 классов: листы 1-3 курс
- База 11 классов: листы 1-2 курс

### Формат данных
- **Группы**: В отдельных колонках (например, БУ1-25, Л1-25, Ф1-25)
- **День недели**: В одной колонке (понедельник, вторник и т.д.)
- **Дата**: В следующей колонке
- **Пары**: Обозначаются римскими цифрами (I, II, III, IV, V, VI, VII)
- **Для каждой пары**:
  - Предмет
  - Кабинет (в следующей колонке)

## Использование бота

### Первый запуск

1. Найдите вашего бота в Telegram
2. Нажмите "Start" или отправьте `/start`
3. Выберите базу образования (9 или 11 классов)
4. Выберите курс
5. Выберите группу

### Команды

- `/start` - Начать работу / изменить группу
- `/help` - Справка

### Главное меню

- **📅 Расписание на сегодня** - Показать расписание на текущий день
- **📆 Расписание на неделю** - Показать расписание на всю неделю
- **⚙️ Настройки** - Настройки уведомлений и смена группы
  - 🔔 Уведомления - Включить/выключить ежедневные уведомления
  - 🔄 Изменить группу - Сменить группу
- **👤 Профиль** - Просмотр информации о профиле
- **❓ Помощь** - Справочная информация

### Уведомления

При включенных уведомлениях бот будет отправлять расписание на следующий день каждый вечер в 18:00 (время настраивается в `.env`).

## Настройки (.env файл)

| Параметр | Описание | По умолчанию |
|----------|----------|--------------|
| `BOT_TOKEN` | Токен Telegram бота от BotFather | Обязательно |
| `DATABASE_PATH` | Путь к файлу базы данных SQLite | `bot_database.db` |
| `SCHEDULE_DIR` | Директория с файлами XLSX расписаний | `.` (текущая директория) |
| `NOTIFICATION_TIME` | Время отправки уведомлений (HH:MM) | `18:00` |

## Обновление расписания

Бот автоматически обновляет расписание:
- При запуске (через 10 секунд после старта)
- Каждый день в 03:00

Для принудительного обновления просто перезапустите бота.

## Логи

Логи сохраняются в двух местах:
- **Консоль** - для мониторинга в реальном времени
- **bot.log** - файл с полной историей

## Решение проблем

### Бот не запускается

**Проблема**: `BOT_TOKEN not found in environment variables`
**Решение**: Создайте файл `.env` и добавьте токен бота

**Проблема**: `ModuleNotFoundError`
**Решение**: Установите зависимости: `pip install -r requirements.txt`

### Нет расписания

**Проблема**: "На сегодня нет занятий или расписание еще не загружено"
**Решение**: 
1. Убедитесь, что XLSX файлы находятся в `SCHEDULE_DIR`
2. Проверьте логи на наличие ошибок парсинга
3. Подождите 10 секунд после запуска бота для автоматической загрузки

### Уведомления не приходят

**Решение**:
1. Проверьте, что уведомления включены в настройках бота
2. Убедитесь, что выбрана группа
3. Проверьте время в `NOTIFICATION_TIME` в `.env`
4. Проверьте логи на наличие ошибок

## Структура проекта

```
tgbot/
├── main.py                 # Точка входа
├── requirements.txt        # Зависимости
├── .env                    # Конфигурация (создать вручную)
├── .env.example           # Пример конфигурации
├── README.md              # Документация
├── SETUP.md               # Это руководство
│
├── database/              # Модуль базы данных
│   ├── __init__.py
│   └── db.py             # SQLite операции
│
├── parser/               # Модуль парсинга XLSX
│   ├── __init__.py
│   └── parser.py        # Парсинг расписаний
│
├── handlers/            # Обработчики команд
│   ├── __init__.py
│   ├── start.py        # /start и регистрация
│   ├── schedule.py     # Расписание
│   ├── settings.py     # Настройки
│   ├── profile.py      # Профиль
│   └── help.py         # Помощь
│
├── keyboards/          # Клавиатуры бота
│   ├── __init__.py
│   ├── main.py        # Основные клавиатуры
│   └── selection.py   # Клавиатуры выбора
│
└── scheduler/         # Планировщик задач
    ├── __init__.py
    └── jobs.py       # Автоматические задачи
```

## Разработка

### Добавление новых функций

1. Создайте новый обработчик в `handlers/`
2. Добавьте его в `main.py`
3. При необходимости создайте новые клавиатуры в `keyboards/`

### Запуск в фоновом режиме (Linux)

```bash
# Использование nohup
nohup python main.py > bot_output.log 2>&1 &

# Использование screen
screen -S tgbot
python main.py
# Нажмите Ctrl+A, затем D для отсоединения

# Использование systemd (рекомендуется для production)
# Создайте файл /etc/systemd/system/tgbot.service
```

### Запуск в Docker

```dockerfile
FROM python:3.10-slim

WORKDIR /app
COPY requirements.txt .
RUN pip install -r requirements.txt

COPY . .

CMD ["python", "main.py"]
```

```bash
docker build -t tgbot .
docker run -d --name tgbot --env-file .env tgbot
```

## Поддержка

При возникновении проблем:
1. Проверьте логи в `bot.log`
2. Убедитесь, что все зависимости установлены
3. Проверьте формат XLSX файлов
4. Создайте Issue в репозитории
