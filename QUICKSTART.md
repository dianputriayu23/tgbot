# БЫСТРЫЙ СТАРТ / QUICK START

## 🚀 Быстрый запуск за 5 минут

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка

Создайте файл `.env` из шаблона:
```bash
cp .env.example .env
```

Отредактируйте `.env` и укажите токен вашего бота:
```env
BOT_TOKEN=123456789:ABCdefGHIjklMNOpqrsTUVwxyz
```

**Как получить токен:**
1. Откройте Telegram и найдите [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям для создания бота
4. Скопируйте полученный токен в `.env`

### 3. Запуск

**Linux/Mac:**
```bash
./start.sh
```

**Windows:**
```bash
python main.py
```

**Или с виртуальным окружением:**
```bash
# Создать окружение
python -m venv venv

# Активировать
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# Установить и запустить
pip install -r requirements.txt
python main.py
```

---

## ✅ Проверка работы

Если всё настроено правильно, вы увидите:

```
INFO - Starting Telegram bot...
INFO - Configuration loaded:
INFO -   Database: schedule.db
INFO -   Schedule folder: .
INFO - Connected to database: schedule.db
INFO - Database initialized successfully
INFO - Performing initial schedule update...
INFO - Found 9 schedule files in .
INFO - Scheduler started
INFO - Bot is ready! Starting polling...
```

---

## 📱 Использование бота

1. Найдите вашего бота в Telegram
2. Отправьте `/start`
3. Выберите курс и группу
4. Готово! Используйте меню для просмотра расписания

---

## 🛠️ Решение проблем

### "BOT_TOKEN not found"
➡️ Создайте файл `.env` и добавьте токен бота

### "Failed to connect to database"
➡️ Проверьте права доступа к папке проекта

### "No module named 'telegram'"
➡️ Установите зависимости: `pip install -r requirements.txt`

### Расписание не отображается
➡️ Убедитесь, что файлы `.xlsx` находятся в корневой папке проекта

---

## 📂 Структура файлов

```
tgbot/
├── main.py              # ← Запускать отсюда
├── requirements.txt     # ← Зависимости
├── .env                 # ← Конфигурация (создать!)
├── start.sh            # ← Скрипт запуска (Linux/Mac)
├── *.xlsx              # ← Файлы расписания
├── database/           # База данных
├── parser/             # Парсер Excel
├── handlers/           # Обработчики команд
├── keyboards/          # Клавиатуры
└── scheduler/          # Уведомления
```

---

## 🎯 Основные возможности

✅ **Просмотр расписания**
- На сегодня/завтра
- На неделю
- На конкретную дату

✅ **Уведомления**
- Автоматическая отправка каждый день
- Настраиваемое время

✅ **Управление**
- Выбор группы
- Настройки профиля
- Справка и помощь

---

## 📖 Команды бота

- `/start` - Начать работу
- `/help` - Справка
- `/changegroup` - Сменить группу
- `/notifications` - Настроить уведомления
- `/about` - О боте

---

## 🔧 Дополнительные настройки

Отредактируйте `.env` для настройки:

```env
# База данных
DATABASE_PATH=schedule.db

# Папка с файлами расписания
SCHEDULE_FOLDER=.

# Время уведомлений по умолчанию
NOTIFICATION_TIME=08:00

# Уровень логирования
LOG_LEVEL=INFO
```

---

## 📞 Поддержка

- 📖 Полная документация: `README_RU.md`
- 🐛 Проблемы: Проверьте логи в `logs/bot.log`

---

**Версия:** 1.0.0  
**Требования:** Python 3.10+
