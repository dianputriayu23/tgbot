# Руководство по развертыванию и запуску бота

## Требования

- Python 3.10 или выше
- pip (установщик пакетов Python)
- Токен Telegram бота (получить у @BotFather)

## Установка

### 1. Клонирование репозитория

```bash
git clone https://github.com/dianputriayu23/tgbot.git
cd tgbot
```

### 2. Создание виртуального окружения (рекомендуется)

```bash
python -m venv venv

# Windows
venv\Scripts\activate

# Linux/Mac
source venv/bin/activate
```

### 3. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 4. Настройка конфигурации

Создайте файл `.env` на основе `.env.example`:

```bash
cp .env.example .env
```

Отредактируйте `.env` и добавьте ваш токен бота:

```
BOT_TOKEN=ваш_токен_от_BotFather
SCHEDULE_URL=https://pkeu.ru/sites/default/files/Files_up_page/
```

### 5. Инициализация базы данных с группами (опционально)

Если у вас есть файлы расписания в формате XLSX, вы можете загрузить группы:

```bash
python populate_groups.py
```

## Запуск бота

```bash
python main.py
```

Бот запустится и будет готов к работе. Вы увидите сообщение в логах:

```
Bot started successfully!
```

## Использование

### Первый запуск

1. Найдите вашего бота в Telegram по имени пользователя
2. Отправьте команду `/start`
3. Выберите базу образования (9 или 11 классов)
4. Выберите курс
5. Выберите вашу группу
6. Готово! Теперь можете просматривать расписание

### Основные команды

- `/start` - Начать работу или сменить группу
- `/help` - Показать справку

### Кнопки меню

- **📅 Сегодня** - Расписание на сегодня
- **📆 Завтра** - Расписание на завтра
- **Пн, Вт, Ср, Чт, Пт, Сб** - Расписание на конкретный день недели
- **👤 Профиль** - Информация о вашем профиле
- **⚙️ Настройки** - Настройки бота и уведомлений
- **❓ Помощь** - Справка по использованию

### Настройки уведомлений

В разделе **Настройки** вы можете включить/выключить:

- **Уведомления о парах** - Утренние напоминания:
  - 7:30 - для 1 и 3 курсов
  - 10:00 - для 2 курса
- **Уведомления об изменениях** - При изменении расписания
- **Уведомления о новом расписании** - Вечерние уведомления (18:00)

## Обслуживание

### Автоматические задачи

Бот автоматически выполняет следующие задачи:

1. **Проверка расписания** - каждые 20 минут
2. **Утренние уведомления** - 7:30 и 10:00
3. **Вечерние уведомления** - 18:00
4. **Очистка старых файлов** - ежедневно в 3:00 (файлы старше 8 дней)

### Логи

Логи сохраняются в файл `bot.log` в директории проекта.

### База данных

База данных SQLite хранится в файле `bot.db`. Резервное копирование:

```bash
cp bot.db bot_backup_$(date +%Y%m%d).db
```

## Структура проекта

```
tgbot/
├── main.py                 # Точка входа
├── requirements.txt        # Зависимости
├── .env                    # Конфигурация (создать вручную)
├── .env.example           # Пример конфигурации
├── bot.db                 # База данных SQLite
├── bot.log                # Логи бота
├── database/
│   ├── __init__.py
│   └── db.py              # Работа с базой данных
├── parser/
│   ├── __init__.py
│   └── parser.py          # Парсинг XLSX файлов
├── handlers/
│   ├── __init__.py
│   ├── start.py           # Регистрация и выбор группы
│   ├── schedule.py        # Просмотр расписания
│   ├── settings.py        # Настройки
│   ├── profile.py         # Профиль пользователя
│   └── help.py            # Справка
├── keyboards/
│   ├── __init__.py
│   ├── main.py            # Главная клавиатура
│   └── settings.py        # Клавиатуры выбора
├── scheduler/
│   ├── __init__.py
│   └── jobs.py            # Автоматические задачи
└── schedules/             # Загруженные файлы расписаний
```

## Устранение неполадок

### Бот не запускается

1. Проверьте, что токен бота указан правильно в `.env`
2. Убедитесь, что все зависимости установлены: `pip install -r requirements.txt`
3. Проверьте логи в `bot.log`

### Бот не отвечает

1. Проверьте, что бот запущен и не завершился с ошибкой
2. Проверьте логи в `bot.log`
3. Убедитесь, что у бота есть доступ к интернету

### Группы не отображаются

1. Запустите `python populate_groups.py` для загрузки групп из файлов
2. Или добавьте группы вручную через базу данных

## Обновление

```bash
git pull
pip install -r requirements.txt --upgrade
```

После обновления перезапустите бота.

## Поддержка

При возникновении проблем проверьте:
1. Логи в файле `bot.log`
2. Наличие всех необходимых файлов
3. Правильность конфигурации в `.env`

## Лицензия

MIT License
